<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Update Patient</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
  <div class="container max-w-lg mx-auto px-4 py-8">
    <h2 class="text-2xl font-bold mb-6 text-center text-gray-800">Update Patient</h2>
    <form action="/patients/update/<%= patient._id %>" method="POST" class="bg-white rounded-lg shadow-lg p-6 space-y-4">
      <div>
        <label class="block font-medium">Name:</label>
        <input type="text" name="name" value="<%= patient.name %>" required class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Age:</label>
        <input type="number" name="age" value="<%= patient.age %>" class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Gender:</label>
        <select name="gender" class="w-full px-3 py-2 border rounded">
          <option value="">Select</option>
          <option value="Male" <%= patient.gender==="Male"?"selected":"" %>>Male</option>
          <option value="Female" <%= patient.gender==="Female"?"selected":"" %>>Female</option>
          <option value="Other" <%= patient.gender==="Other"?"selected":"" %>>Other</option>
        </select>
      </div>
      <div>
        <label class="block font-medium">Contact Number:</label>
        <input type="text" name="contactNumber" value="<%= patient.contactNumber %>" class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Address:</label>
        <input type="text" name="address" value="<%= patient.address %>" class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Blood Group:</label>
        <input type="text" name="bloodGroup" value="<%= patient.bloodGroup %>" class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Medical History (comma separated):</label>
        <input type="text" name="medicalHistory" value="<%= patient.medicalHistory?.join(',') %>" class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Assign Doctor:</label>
        <select name="doctorAssigned" class="w-full px-3 py-2 border rounded">
          <option value="">None</option>
          <% doctors.forEach(d => { %>
            <option value="<%= d._id %>" <%= patient.doctorAssigned && String(patient.doctorAssigned._id || patient.doctorAssigned) === String(d._id) ? "selected" : "" %>><%= d.name %> - <%= d.specialization %></option>
          <% }) %>
        </select>
      </div>
      <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white py-2 rounded">Update Patient</button>
    </form>
    <div class="mt-6 text-center">
      <a href="/patients/view" class="text-gray-700 hover:underline">← Back to Patient List</a>
    </div>
  </div>
</body>
</html>
