<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add Patient</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
  <div class="container max-w-lg mx-auto px-4 py-8">
    <h2 class="text-2xl font-bold mb-6 text-center text-gray-800">Add Patient</h2>
    <form action="/patients/add" method="POST" class="bg-white rounded-lg shadow-lg p-6 space-y-4">
      <div>
        <label class="block font-medium">Name:</label>
        <input type="text" name="name" required class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Age:</label>
        <input type="number" name="age" class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Gender:</label>
        <select name="gender" class="w-full px-3 py-2 border rounded">
          <option value="">Select</option>
          <option>Male</option>
          <option>Female</option>
          <option>Other</option>
        </select>
      </div>
      <div>
        <label class="block font-medium">Contact Number:</label>
        <input type="text" name="contactNumber" class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Address:</label>
        <input type="text" name="address" class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Blood Group:</label>
        <input type="text" name="bloodGroup" class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Medical History (comma separated):</label>
        <input type="text" name="medicalHistory" class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Assign Doctor:</label>
        <select name="doctorAssigned" class="w-full px-3 py-2 border rounded">
          <option value="">None</option>
          <% if (typeof doctors !== "undefined") { %>
            <% doctors.forEach(d => { %>
              <option value="<%= d._id %>"><%= d.name %> - <%= d.specialization %></option>
            <% }) %>
          <% } %>
        </select>
      </div>
      <hr>
      <div>
        <label class="block font-medium">Username (for login):</label>
        <input type="text" name="username" required class="w-full px-3 py-2 border rounded" />
      </div>
      <div>
        <label class="block font-medium">Password (for login):</label>
        <input type="password" name="password" required minlength="4" class="w-full px-3 py-2 border rounded" />
      </div>
      <button type="submit" class="w-full bg-indigo-600 hover:bg-indigo-700 text-white py-2 rounded">Add Patient</button>
    </form>
    <div class="mt-6 text-center">
      <a href="/patients/view" class="text-gray-700 hover:underline">← Back to Patient List</a>
    </div>
  </div>
</body>
</html>