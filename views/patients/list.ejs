<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Patient List</title>

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 text-gray-800 font-sans p-6">

  <div class="mx-auto bg-white rounded-lg shadow p-8">
    <h2 class="text-2xl font-bold mb-6 text-blue-700">📋 Patient List</h2>

    <ul class="flex flex-wrap gap-3">
  <% patients.forEach(patient => { %>
  <li class="border border-gray-200 p-4 rounded flex justify-between items-center w-[275px]">
    <div>
      <span class="font-medium text-lg"><%= patient.name %></span><br>
      <span class="text-gray-600"><%= patient.contactNumber %></span>
    </div>
    <a href="/patients/update/<%= patient._id %>" 
       class="bg-blue-500 hover:bg-blue-600 text-white px-3 py-1 rounded text-sm">
      Edit
    </a>
  </li>
<% }) %>

</ul>


    <a href="/" class="mt-6 inline-block bg-blue-600 text-white px-5 py-2 rounded hover:bg-blue-700 transition">
      🏠 Back to Home
    </a>
  </div>

</body>
</html>